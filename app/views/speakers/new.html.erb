

<% if notice.present? %>
  <div class="alert alert-asoban">
    <a href="#" class="close" data-dismiss="alert" aria-label="close">&times;</a>
    <div id="notice"> <%= notice %> </div>
  </div>
<% end %>

<div class="row">
  <div class="col-xs-8 col-xs-offset-2">
    <div class="box">
      <div class="box-header">
				<h2>Ingresar nuevo conferencista del evento "<%= @event.title %>"</h2>
      </div>
      <!-- /.box-header -->
      <div class="box-body">
        <%= render 'form', speaker: @speaker %>
      </div>
      <!-- /.box-body -->
      <div class="box-footer">
        <%= link_to 'Volver a edición evento', edit_event_path(@event), class: 'btn btn-asoban pull-right' %>
      </div>
    </div>
    <!-- /.box -->
  </div>
</div>
