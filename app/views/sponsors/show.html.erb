<p id="notice"><%= notice %></p>

<p>
  <strong>Image:</strong>
  <%= @sponsor.image %>
</p>

<p>
  <strong>Event:</strong>
  <%= @sponsor.event.title %>
</p>

<%= link_to 'Edit', edit_event_sponsor_path(@event, @sponsor) %> |
<%= link_to 'Back', event_sponsors_path %>
